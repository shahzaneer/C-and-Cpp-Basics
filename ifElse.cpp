#include <iostream>
using namespace std;

int main(){
    cout<<"Enter a number \n";
    int num;
    cin>>num; //cin for input >> for extraction
    if(num%2==0){
        cout<<"Even"; //cout for output << for insertion
    }
    else{
        cout<<"Odd";
    }
    // we can also use nested if - else like java..
    
}